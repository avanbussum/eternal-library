import blockContent from './blockContent'
import category from './category'
import post from './post'
import author from './author'
import quote from './quote'

export const schemaTypes = [post, author, category, blockContent, quote]
